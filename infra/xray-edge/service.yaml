apiVersion: v1
kind: Service
metadata:
  name: xray-edge
  namespace: infra
spec:
  selector:
    app: xray-edge
  ports:
    - name: socks
      port: 10808
      targetPort: 10808
    - name: http
      port: 10809
      targetPort: 10809
  type: ClusterIP
