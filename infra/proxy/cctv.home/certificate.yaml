apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cctv-home-internal-tls
  namespace: infra
spec:
  secretName: cctv-home-internal-tls
  issuerRef:
    kind: ClusterIssuer
    name: root-ca-issuer
  dnsNames:
    - cctv.home
