apiVersion: v1
kind: Service
metadata:
  name: cctv-home-proxy
  namespace: infra
spec:
  clusterIP: None        # headless, чтобы Traefik ходил прямо в Endpoints
  ports:
    - name: https
      port: 8971
      targetPort: 8971
