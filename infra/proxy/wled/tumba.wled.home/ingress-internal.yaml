apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tumba-wled-home-internal
  namespace: infra
  annotations:
    cert-manager.io/cluster-issuer: root-ca-issuer
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/redirect-entry-point: websecure
    traefik.ingress.kubernetes.io/router.middlewares: infra-redirect-to-https@kubernetescrd
spec:
  rules:
  - host: tumba.wled.home
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: tumba-wled-home-proxy
            port:
              number: 80
  tls:
  - hosts:
    - tumba.wled.home
    secretName: tumba-wled-home-internal-tls
