apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: stash
  namespace: infra
spec:
  interval: 1h
  chart:
    spec:
      chart: stash
      version: v2024.3.11
      sourceRef:
        kind: HelmRepository
        name: appscode
        namespace: flux-system
  values:
    features:
      community: true
    license: ""
