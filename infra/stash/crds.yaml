apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: stash-crds
  namespace: infra
spec:
  chart:
    spec:
      chart: stash-crds
      sourceRef:
        kind: HelmRepository
        name: appscode
        namespace: flux-system
      version: v2025.6.30
  interval: 1h
  install:
    createNamespace: false
    crds: Create
  upgrade:
    crds: CreateReplace
