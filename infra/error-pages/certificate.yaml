apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-progist-ru
  namespace: infra
spec:
  secretName: wildcard-progist-ru-tls
  issuerRef:
    name: letsencrypt-prod-dns
    kind: ClusterIssuer
  dnsNames:
    - "*.progist.ru"
