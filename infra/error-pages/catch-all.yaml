apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: error-pages-catch-all
  namespace: infra
spec:
  entryPoints:
    - websecure
  routes:
    - match: HostRegexp(`^[a-zA-Z0-9-]+\.progist\.ru$`)
      kind: Rule
      priority: 1
      middlewares:
        - name: error-pages
          namespace: infra
      services:
        - name: error-pages
          port: 8080
          namespace: infra
  tls:
    secretName: wildcard-progist-ru-tls
