apiVersion: v1
kind: Service
metadata:
  name: xray
  namespace: infra
spec:
  selector:
    app: xray
  ports:
    - name: http
      port: 10808
      targetPort: 10808
    - name: socks
      port: 10809
      targetPort: 10809
  type: ClusterIP
