apiVersion: apps/v1
kind: Deployment
metadata:
  name: xray
  namespace: infra
spec:
  replicas: 1
  selector:
    matchLabels:
      app: xray
  template:
    metadata:
      labels:
        app: xray
    spec:
      containers:
        - name: xray
          image: teddysun/xray:26.4.25
          volumeMounts:
            - name: config
              mountPath: /etc/xray
              readOnly: true
          ports:
            - containerPort: 10808
            - containerPort: 10809
          livenessProbe:
            tcpSocket:
              port: 10809
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 10809
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
          resources:
            requests:
              cpu: "25m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
      volumes:
        - name: config
          secret:
            secretName: xray-config
