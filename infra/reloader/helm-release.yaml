apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: reloader
  namespace: infra
spec:
  interval: 1h
  chart:
    spec:
      chart: reloader
      version: "1.0.15"
      sourceRef:
        kind: HelmRepository
        name: reloader
        namespace: flux-system
