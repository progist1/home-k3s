apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strict-chain
  namespace: infra
spec:
  chain:
    middlewares:
      - name: redirect-to-https
        namespace: infra
      - name: ratelimit-strict
        namespace: infra

