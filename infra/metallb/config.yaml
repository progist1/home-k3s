apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: metallb-config
  namespace: flux-system
spec:
  interval: 10m
  prune: true
  path: ./infra/metallb/step2-config
  dependsOn:
    - name: metallb-install
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
