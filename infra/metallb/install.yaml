apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: metallb-install
  namespace: flux-system
spec:
  interval: 10m
  prune: true
  path: ./infra/metallb/step1-install
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
