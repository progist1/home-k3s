apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: traefik-adv
  namespace: metallb
spec:
  ipAddressPools:
    - traefik-pool
  nodeSelectors:
    - matchLabels:
        kubernetes.io/hostname: k3s-prod
    - matchLabels:
        kubernetes.io/hostname: k3s-pi
