configMapGenerator:
  - name: kps-values
    namespace: monitoring
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
resources:
  - scrapeconfigs
  - helmrelease.yaml
  - alert-manager-pv.yaml
  - grafana-pvc.yaml
  - exporters
  - rules
  - data.yaml
  - pushgateway
