configMapGenerator:
  - name: kps-values
    namespace: monitoring
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
resources:
  - configs
  - helmrelease.yaml
  - alert-manager-pv.yaml
  - grafana-pvc.yaml
