configMapGenerator:
  - name: kps-values
    namespace: monitoring
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
resources:
  - scrapeconfigs
  - ingress-external-grafana.yaml
  - helmrelease.yaml
  - alert-manager-pv.yaml
  - grafana-pvc.yaml
  - loki-datasource.yaml
  - exporters
  - rules
  - data.yaml
  - pushgateway
  - mailtest
