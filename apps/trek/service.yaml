apiVersion: v1
kind: Service
metadata:
  name: trek
  namespace: apps
  labels:
    app.kubernetes.io/name: trek
    managed-by: flux
spec:
  selector:
    app: trek
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP
