apiVersion: v1
kind: Service
metadata:
  name: pgvecto
  namespace: apps
spec:
  selector:
    app: pgvecto
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
      nodePort: 30700
  type: NodePort
