apiVersion: v1
kind: Service
metadata:
  name: mailu-webmail
  namespace: apps
spec:
  internalTrafficPolicy: Cluster
  selector:
    app: mailu-webmail
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  type: ClusterIP