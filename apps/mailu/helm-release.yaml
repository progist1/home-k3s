apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mailu
  namespace: apps
spec:
  interval: 5m
  chart:
    spec:
      chart: mailu
      version: "2.2.2"
      sourceRef:
        kind: HelmRepository
        name: mailu-charts
        namespace: flux-system
      valuesFiles:
        - ./values.yaml
