namespace: apps

resources:
  - storage.yaml
  - helm-release.yaml
  - ingress-internal.yaml
  - ingress-external.yaml

configMapGenerator:
  - name: mailu-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
