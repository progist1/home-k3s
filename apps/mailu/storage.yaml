apiVersion: v1
kind: PersistentVolume
metadata:
  name: mailu-pv
spec:
  storageClassName: nfs-mailu
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteMany
  nfs:
    path: /mnt/Fast/k3s-data/mailu
    server: 10.0.0.4
  persistentVolumeReclaimPolicy: Retain
