apiVersion: v1
kind: Service
metadata:
  name: mailu-front
  namespace: apps
spec:
  internalTrafficPolicy: Cluster
  type: ExternalName
  externalName: 127.0.0.1
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
