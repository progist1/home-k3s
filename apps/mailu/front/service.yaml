apiVersion: v1
kind: Service
metadata:
  name: mailu-front
  namespace: apps
spec:
  internalTrafficPolicy: Cluster
  selector:
    app: mailu-front
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP