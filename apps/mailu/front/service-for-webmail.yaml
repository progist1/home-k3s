apiVersion: v1
kind: Service
metadata:
  name: mailu-front-webmail
  namespace: apps
spec:
  selector:
    app: mailu-front
  ports:
  - name: imap-auth
    port: 10143
    protocol: TCP
  - name: smtp-auth
    port: 10025
    protocol: TCP
  type: ClusterIP
