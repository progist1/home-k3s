apiVersion: v1
kind: Service
metadata:
  name: mailu-admin
  namespace: apps
spec:
  internalTrafficPolicy: Cluster
  selector:
    app: mailu-admin
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP