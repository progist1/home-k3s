apiVersion: v1
kind: ConfigMap
metadata:
  name: adguard-config
  namespace: apps
data:
  AdGuardHome.yaml: |
    bind_host: 0.0.0.0
    bind_port: 3000
    dns:
      bind_hosts:
      - 0.0.0.0
      port: 5353
