apiVersion: v1
kind: Service
metadata:
  name: adguard-home-ui
  namespace: apps
spec:
  type: LoadBalancer
  loadBalancerIP: 10.0.0.2
  ports:
  - name: http
    port: 3000
    targetPort: 3000
  selector:
    app: adguard-home
