apiVersion: v1
kind: Service
metadata:
  name: portainer
  namespace: apps
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 9000
    protocol: TCP
    name: http
  selector:
    app: portainer
