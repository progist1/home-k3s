apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: apps
spec:
  type: NodePort
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      nodePort: 30032
  selector:
    app: postgres
