apiVersion: v1
kind: Service
metadata:
  name: postgres-nodeport
  namespace: apps
spec:
  type: NodePort
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      nodePort: 30032
  selector:
    app.kubernetes.io/instance: postgres
