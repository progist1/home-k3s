apiVersion: v1
kind: Service
metadata:
  name: photoprism
  namespace: apps
spec:
  selector:
    app: photoprism
  ports:
    - protocol: TCP
      port: 80
      targetPort: 20800
  type: ClusterIP
