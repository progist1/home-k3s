apiVersion: v1
kind: Service
metadata:
  name: readmeabook
  namespace: apps
spec:
  selector:
    app: readmeabook
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3030
  type: ClusterIP
