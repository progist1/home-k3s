apiVersion: v1
kind: ConfigMap
metadata:
  name: vikunja-config
  namespace: apps
data:
  config.yml: |
    auth:
      local:
        enabled: false
      openid:
        enabled: true
        providers:
          - name: "Progist Auth"
