apiVersion: v1
kind: Service
metadata:
  name: gitlab-ssh
  namespace: gitlab
spec:
  selector:
    app: gitlab
  ports:
  - name: ssh
    protocol: TCP
    port: 2222
    targetPort: 22
  type: NodePort
  externalTrafficPolicy: Local
